# Session 2026-09-09

Continued CPU/graphics work with the user's explicit audio focus. Prior code
checkpoint 66aa962 includes the standalone SPU2 ADPCM decoder and VSUB
broadcast support. User research files and BIOS remain untouched and local.

## Previous replay result

`build-release/vsub-audio-300m.txt` stopped at step 252573714,
PC 0027340C, opcode 4BE43190 (VMAXx), 1543 instructions past the earlier
stop. Graphics unchanged: hash 301EB407483F092F, 9664 RGB pixels,
6275 triangles, 160 accepted VIF submissions, 998 VU1 pairs.

Each SPU2 core has four nonzero key-on store attempts with union mask FFFFFF.
This may be initialization and is not proof of a boot-sound request. There is
still no connected voice mixer or Vita audio output. ADPCM decode tests are
not a claim that BIOS audio plays.

## VU min/max and audio probes

Added VMAX/VMINI x/y/z/w using integer ordering of float bit patterns, rather
than host NaN-sensitive min/max. Broadcast scalars are captured before writes.
Tests cover captured opcode, aliases, signed zero, negative values, subnormal
and exceptional bit patterns, destination masks, VF0, and unchanged STATUS.
Reference: [VU min/max semantics](https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/VUops.cpp).

Read game-audio skill again for continuing audio work. Kept the focus on
platform-specific sample decoding and evidence, not placeholder sound design.
Added bounded first-four-key-on-attempt probes per core to the BIOS trace:
SSA, pitch, ADSR words, ADPCM header/flags, decode success and raw sample peak.
The probe reads the voice's start block from SPU2 RAM with fresh history; it
does not apply envelopes, interpolation or volume, nor simulate a voice.
These are pre-store observations, not proof of completed key-on or audibility.
Reference: [SPU2 registers](https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/SPU2/regs.h).

Replay completed: `build-release/vmax-audio-300m.txt`, image `build-release/vmax-audio.ppm`.

Release and UBSan CTest pass 2/2; Vita cross-build passes.
VPK SHA-256:
`5721DB5D34886402D0082798A5CE02770A5424FA988E31FE88EC8313C06E9E6F`.

## 300M integration result and audio evidence

Reached the full 300000000-step ceiling without an unsupported CPU opcode;
trace reports reason=running, PC 00272728, opcode 2443000C. The tool exits 1
because it did not hit a requested stop PC, not because it found a new invalid
instruction. Accepted all 5362 VIF submissions, emitted 69219 triangles and
24 lines. VU1 remains at 998 pairs with the historical 8/1 PATH1 tag record.

Framebuffer hash A02E6325E02ACB83, 10240 nonzero RGB pixels. Converted the PPM
to PNG and inspected it: flat dark-blue upper region and black below. No
recognizable PS2 intro, text or menu. This proves sustained execution through
the tested window, not graphics accuracy, game compatibility or Vita speed.

Audio: still four nonzero key-on store attempts per core, union FFFFFF.
The bounded probe captured 96 voice-start observations; all decode with zero
raw peak, SSA 02800, pitch 3FFF and ADSR 0000/0000. Events occur during early
initialization around steps 179185263..179774359. No later nonzero key-on
attempt was observed through 300M. Do not describe the initialization masks
as an audible boot request. Decoder exists, but voice scheduling/envelopes,
mixing and Vita output remain unimplemented.

## Resume priorities

1. Diagnose the sustained graphics workload rather than hunt a nonexistent
   next opcode failure at 300M: compare GS state/primitive output and inspect
   texture, depth, blending and raster coordinates. Keep the earlier PATH1
   timing rejection separate from the now-working VIF transport/DIRECT path.
2. Continue audio voice-state integration with tested address progression,
   loop/end handling, envelopes and sample timing before device output.
   Preserve initialization silence; never inject substitute boot audio.
3. Save exact replay/image/audio results at each checkpoint; no reliable
   percentage-to-intro or physical-device performance claim is available.

Code checkpoint 0c5cb9f is pushed. All current build/replay processes finished;
user research files and BIOS remain local and untouched.

## GS blend equation checkpoint

Implemented the defined ALPHA A/B/C/D selectors and FIX coefficient in the
software pixel path: (A-B)*C/128+D, signed floor scaling, RGB clamp/wrap,
source-alpha preservation and PABE bypass. GIF now retains both ALPHA contexts
and global PABE/COLCLAMP, with PRIM.ABE enabling the operation per draw.
Reserved selector 3 is not modeled. This is not complete GS framebuffer-format,
alpha-test, texture or rasterization support.

Regression coverage includes unity/half source alpha, destination alpha, FIX,
negative rounding, overflow clamp/wrap, disabled blending, PABE and GIF register
routing across both contexts. Release and UBSan suites pass 2/2 each; Vita VPK
build succeeds. Reference used for equation semantics:
https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/GS/Renderers/SW/GSDrawScanlineCodeGenerator.all.cpp

A fresh 300M replay is being captured locally as build-release/blend-300m.txt
and .ppm. Its visual outcome is not yet established at this checkpoint.
No audio behavior changed; the initialization-silence limitations above remain.
