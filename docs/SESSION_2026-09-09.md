# Session 2026-09-09

Continued CPU/graphics work with the user's explicit audio focus. Prior code
checkpoint 66aa962 includes the standalone SPU2 ADPCM decoder and VSUB
broadcast support. User research files and BIOS remain untouched and local.

## Previous replay result

`build-release/vsub-audio-300m.txt` stopped at step 252573714,
PC 0027340C, opcode 4BE43190 (VMAXx), 1543 instructions past the earlier
stop. Graphics unchanged: hash 301EB407483F092F, 9664 RGB pixels,
6275 triangles, 160 accepted VIF submissions, 998 VU1 pairs.

Each SPU2 core has four nonzero key-on store attempts with union mask FFFFFF.
This may be initialization and is not proof of a boot-sound request. There is
still no connected voice mixer or Vita audio output. ADPCM decode tests are
not a claim that BIOS audio plays.

## VU min/max and audio probes

Added VMAX/VMINI x/y/z/w using integer ordering of float bit patterns, rather
than host NaN-sensitive min/max. Broadcast scalars are captured before writes.
Tests cover captured opcode, aliases, signed zero, negative values, subnormal
and exceptional bit patterns, destination masks, VF0, and unchanged STATUS.
Reference: [VU min/max semantics](https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/VUops.cpp).

Read game-audio skill again for continuing audio work. Kept the focus on
platform-specific sample decoding and evidence, not placeholder sound design.
Added bounded first-four-key-on-attempt probes per core to the BIOS trace:
SSA, pitch, ADSR words, ADPCM header/flags, decode success and raw sample peak.
The probe reads the voice's start block from SPU2 RAM with fresh history; it
does not apply envelopes, interpolation or volume, nor simulate a voice.
These are pre-store observations, not proof of completed key-on or audibility.
Reference: [SPU2 registers](https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/SPU2/regs.h).

Replay completed: `build-release/vmax-audio-300m.txt`, image `build-release/vmax-audio.ppm`.

Release and UBSan CTest pass 2/2; Vita cross-build passes.
VPK SHA-256:
`5721DB5D34886402D0082798A5CE02770A5424FA988E31FE88EC8313C06E9E6F`.

## 300M integration result and audio evidence

Reached the full 300000000-step ceiling without an unsupported CPU opcode;
trace reports reason=running, PC 00272728, opcode 2443000C. The tool exits 1
because it did not hit a requested stop PC, not because it found a new invalid
instruction. Accepted all 5362 VIF submissions, emitted 69219 triangles and
24 lines. VU1 remains at 998 pairs with the historical 8/1 PATH1 tag record.

Framebuffer hash A02E6325E02ACB83, 10240 nonzero RGB pixels. Converted the PPM
to PNG and inspected it: flat dark-blue upper region and black below. No
recognizable PS2 intro, text or menu. This proves sustained execution through
the tested window, not graphics accuracy, game compatibility or Vita speed.

Audio: still four nonzero key-on store attempts per core, union FFFFFF.
The bounded probe captured 96 voice-start observations; all decode with zero
raw peak, SSA 02800, pitch 3FFF and ADSR 0000/0000. Events occur during early
initialization around steps 179185263..179774359. No later nonzero key-on
attempt was observed through 300M. Do not describe the initialization masks
as an audible boot request. Decoder exists, but voice scheduling/envelopes,
mixing and Vita output remain unimplemented.

## Resume priorities

1. Diagnose the sustained graphics workload rather than hunt a nonexistent
   next opcode failure at 300M: compare GS state/primitive output and inspect
   texture, depth, blending and raster coordinates. Keep the earlier PATH1
   timing rejection separate from the now-working VIF transport/DIRECT path.
2. Continue audio voice-state integration with tested address progression,
   loop/end handling, envelopes and sample timing before device output.
   Preserve initialization silence; never inject substitute boot audio.
3. Save exact replay/image/audio results at each checkpoint; no reliable
   percentage-to-intro or physical-device performance claim is available.

Code checkpoint 0c5cb9f is pushed. All current build/replay processes finished;
user research files and BIOS remain local and untouched.

## GS blend equation checkpoint

Implemented the defined ALPHA A/B/C/D selectors and FIX coefficient in the
software pixel path: (A-B)*C/128+D, signed floor scaling, RGB clamp/wrap,
source-alpha preservation and PABE bypass. GIF now retains both ALPHA contexts
and global PABE/COLCLAMP, with PRIM.ABE enabling the operation per draw.
Reserved selector 3 is not modeled. This is not complete GS framebuffer-format,
alpha-test, texture or rasterization support.

Regression coverage includes unity/half source alpha, destination alpha, FIX,
negative rounding, overflow clamp/wrap, disabled blending, PABE and GIF register
routing across both contexts. Release and UBSan suites pass 2/2 each; Vita VPK
build succeeds. Reference used for equation semantics:
https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/GS/Renderers/SW/GSDrawScanlineCodeGenerator.all.cpp

A fresh 300M replay completed locally as build-release/blend-300m.txt and .ppm.
Framebuffer hash is 855B2EF0C4BB8953 with 10240 nonzero RGB pixels; all 5362 VIF
submissions are accepted and 69219 triangles emitted. Inspected the converted
PNG: black/gray rectangular regions with scattered blue horizontal fragments,
not a recognizable PS2 intro. Blending changes the output, but does not resolve
the remaining rendering defects. Both cores still report four key-on attempts
and no nonzero raw-peak probe. The replay process has finished.
Vita VPK SHA256: 602A1D0C5228FD4C8FDF3D13F73DC06763BC1209A5AC8E9A81B2A3C51F82750A.
No audio behavior changed; the initialization-silence limitations above remain.

## Source alpha-test checkpoint

Added TEST.ATE/ATST/AREF comparisons and AFAIL KEEP, FB_ONLY, ZB_ONLY and
RGB_ONLY handling in the software pixel path. Alpha failure now independently
masks color/depth writes; RGB_ONLY preserves destination alpha in the current
32-bit host color buffer. Depth rejection still applies to permitted writes.
GIF selects TEST from the active context on each drawing kick.

Tests cover all eight comparisons at values below/equal/above the reference,
all four failure actions, depth-write visibility through a later draw, depth
rejection and both GIF TEST contexts. Release and UBSan suites pass 2/2;
Vita package builds. Register definitions checked against:
https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/GS/GSRegs.h

No fresh 300M replay was run for this checkpoint. The first captured triangles
use TEST=50000 (ATE disabled), so this change does not explain their malformed
geometry. Destination-alpha testing and framebuffer-format-specific behavior
remain unimplemented. The previous inspected blend-300m image remains the
latest visual evidence; no new intro/audio claim. Audio code is unchanged.

## Functional ADPCM stream checkpoint

Added Spu2AdpcmStream on top of the block decoder. It tracks aligned 20-bit
word addresses, advances eight words per block with RAM wrap, retains predictor
history, records encoded loop starts unless explicitly overridden, repeats on
END+LOOP and stops on nonrepeating END. Invalid predictors and inactive streams
leave output/state untouched. Restart resets history and the manual-loop latch.

This is a functional block-level helper, not an emulated hardware voice. It is
not connected to guest key-on registers or a mixer. Its encountered_end flag is
not a cycle-accurate ENDX event, and next_word_address is not hardware NAX.
Reference semantics (including the hardware timing this helper does NOT model):
https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/SPU2/Mixer.cpp

Regression tests cover initial silence/inactivity, address alignment and wrap,
cross-block predictor history, loop-start capture, repeat/end, manual override,
malformed-block atomicity and restart. Game-audio skill guidance was applied to
keep platform validation explicit; no substitute boot sound was introduced.
No fresh BIOS replay or audible playback claim accompanies this helper.
Release and UBSan suites pass 2/2 each; Vita VPK build succeeds. All build
processes finished. Next audio step remains timed voice/envelope integration.

## DMA-backed audio stream integration

Connected the functional stream decoder to read-only SPU2 sample RAM. Both DMA4
and DMA7 integration fixtures upload two ADPCM blocks across the 2 MiB RAM end,
then verify PCM values, predictor history and nonrepeating termination through
the actual Memory reader. Release and UBSan suites pass 2/2; Vita VPK builds.

The existing bounded key-on diagnostic now also reports an eight-block
spu2_stream_probe. This is lookahead into RAM at the store attempt, with fresh
history and the functional default loop address. It does not simulate elapsed
voice time, guest LSA overrides, later DMA, pitch, ADSR, key-off or audible gain.
No guest register semantics changed. Game-audio platform-validation guidance
keeps this diagnostic explicitly separate from playback.

A focused 180M BIOS replay completed as build-release/audio-stream-180m.txt:
reason=running, EE PC 00081FC4, IOP PC 00098890 at 22500000 IOP cycles.
All 96 stream probes decode eight blocks with raw_peak=0, next_word=02840,
end=0, active=1. Four nonzero key-on attempts per core, masks FFFFFF, as before.
The observed initialization data is silent beyond just its first block; this
does not establish whether later boot audio is requested. Tool exit 1 is the
step ceiling, not an unsupported instruction. All processes finished. No new
graphics capture; the prior visual evidence remains unchanged.

## Functional ADSR envelope checkpoint

Added Spu2Envelope, clocked explicitly once per audio sample, with ADSR1/2
field decoding, attack/decay/sustain/release phases, fractional rate counter,
linear/exponential modes, level saturation and retrigger/release resets.
Negative scaling uses portable integer floor division instead of signed shifts.
Tests exercise exact fast-attack/decay values, two-sample slow attack, exponential
attack slowdown, linear/exponential release, both sustain directions and reset.

The helper is NOT wired to guest KON/KOFF, ENVX or the scheduler. Extreme-rate
"infinite" settings are unverified; current minimum counter increment is one.
No hardware key-on delay or audible output is claimed. The game-audio skill's
platform-validation guidance keeps these limitations explicit.
Semantic references:
https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/SPU2/ADSR.cpp
https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/SPU2/defs.h

No BIOS replay needed to claim only this isolated helper's test coverage; prior
180M silence evidence and prior graphics image remain the latest integration
results. Next step is voice scheduling and register integration, then mixing.
Release and UBSan suites pass 2/2 each; Vita package builds. All processes ended.

## Functional mono voice integration

Added Spu2Voice joining DMA-backed ADPCM streaming, envelope gain and a 12-bit
fractional pitch cursor. It drains buffered terminal-block samples, releases on
key_off, resets on key_on and fails silent with an explicit decode-error flag.
Regression fixtures exercise real DMA-loaded PCM, exact attack-scaled values,
terminal draining, half/double/zero pitch, release and malformed predictor data.

This is a functional test path, NOT guest playback: sample holding replaces
Gaussian interpolation, startup timing is simplified, and there is no guest
KON/KOFF binding, ENVX/NAX/ENDX timing, volume/routing/mixer/reverb or Vita audio
output. No new BIOS replay or intro/audio improvement is claimed. Game-audio
validation guidance was used to preserve these platform-specific boundaries.

Next: verify/register-bind a scheduled voice bank, preserving known BIOS
initialization silence; accurate interpolation and mixing remain required.
Release and UBSan suites pass 2/2 each; Vita builds. All build processes finished.

## Register-driven shadow voice bank

Memory now offers opt-in shadow SPU2 execution, disabled by default and enabled
by bios_trace. Completed register byte writes drive both cores' KON/KOFF masks;
SSA, pitch and ADSR feed 48 functional voices. The shadow clock ticks every
6144 EE cycles (768 IOP cycles), with a two-tick key-on delay. LSA writes update
functional loop overrides. No guest register readback, IRQ or device output is
changed. bios_trace reports ticks, pre-mix peak, active voices and decode errors.

Tests cover boundary splitting, delay, both cores/high mask bits, ignored padding,
KOFF, disabling/reset, unchanged ENVX and a nonzero DMA -> KON -> envelope sample.
The cadence/delay reference is:
https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/SPU2/spu2sys.cpp

Limitations: functional sample holding, approximate envelope extreme rates,
unverified KON/KOFF race behavior and LSA writes during startup; no volume gates,
master volume, routing, mixing, reverb, Gaussian filtering or hardware NAX/ENDX.
Shadow processing happens after device advancement; callers that batch across
DMA boundaries must not treat its historical sample ordering as accurate.
This remains a diagnostic bank, not working PS2 audio. No new BIOS replay yet.

Progress reporting: no defensible overall completion percentage. Verified host
execution and component tests do not establish intro rendering, audible boot,
game compatibility or physical Vita performance. Next integration check: replay
180M with the shadow enabled and compare PCs/key-on counts/silence to baseline.
Release and UBSan suites pass 2/2 each; Vita package builds; all processes ended.

## Scheduled shadow BIOS replay verified

Completed build-release/shadow-audio-180m.txt at 180000000 instructions:
reason=running, EE PC 00081FC4/opcode 00000000; IOP 22500000 cycles,
PC 00098890/opcode 28420C34. Reported CPU state and four key-on attempts per
core (FFFFFF mask) match the prior audio-stream-180m baseline.

Shadow summary: ticks=28620, pre_mix_peak=0, active=48, decode_errors=0.
These functional initialization voices remain silent when scheduled over time;
active=48 is not evidence of audible sound. Shadow ticks count Memory::advance
time, not the trace's instruction counter. No guest-visible SPU2 state or real
audio output is enabled. This does not establish later boot-sound behavior.

Added regressions for split-versus-batched advancement without intervening DMA
or writes, fractional clock carry, cancellation of a queued start under the
current functional policy, and reset/disabling. Release and UBSan pass 2/2.
No production code changed this checkpoint; prior Vita build remains applicable.
All processes finished. Game-audio validation guidance kept diagnostic peaks
separate from audible-output claims. Next work remains timing fidelity, mixing
and device output, alongside the unresolved intro rendering.

## Fixed-volume dry mixing diagnostic

Added signed fixed voice-volume scaling and per-core stereo VMIXL/VMIXR routing
to the opt-in shadow bank. A wide integer sum is retained before master volume;
the diagnostic reports the latest dry sample per core/channel. Volume sweep
mode is explicitly unsupported: routed sweep voices are flagged in a sticky
48-bit mask and omitted from the dry sum, not treated as fixed gain.

Tests cover mute, half gain, sign inversion, negative rounding, the +32768
wide endpoint, unsupported sweep atomicity, DMA-backed stereo routing, core
isolation and independent channel disabling. Sources for fixed gain/registers:
https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/SPU2/ADSR.cpp
https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/SPU2/regs.h

No master volume, MMIX/core output gates, wet/reverb path, external input,
cross-core routing, final clipping, volume sweeps or device output is modeled.
Latest dry samples are not peak statistics or audible-output proof. No fresh
BIOS replay this checkpoint; previous scheduled-silence evidence still applies
only to the previous replay. Game-audio validation guidance kept that distinction.
Release and UBSan pass 2/2 each; Vita builds. All processes finished.

## VU1 packet-store provenance

Added opt-in bounded SQ/SQI write records (issue-pair index, micro PC, VU data
byte offset, value), plus the first rejected tag's pair index. The standalone
VIF replay enables tracing and prints writes spanning the rejected packet prefix.
Capacity is 4096 records with an explicit dropped count; reset clears records,
and disabling tracing stops collection. Tests cover values, bounds and reset.

Local store-provenance-first-vif.txt: 348 records, zero dropped, rejection at
pair 52/PC C0. Address 160 initially receives 420F028B at pair17/PC6A8. The next
tag at 150 is written only at pair75/PC178, as 302E600000008004 with descriptor
412. Address160 is overwritten at pair110/PC2C0 with STQ-like float words.
The trace therefore establishes later writes to the packet being read by the
current instantaneous XGKICK. It does NOT establish the correct transfer delay:
issue pairs are not cycles, dependency stalls and XGKICK arbitration remain
unmodeled. Do not insert a fixed 23-pair/BIOS-specific wait to fit this trace.

Standalone replay remains 998 pairs, PATH1 8/1, two triangles. No renderer or
guest behavior change intended; audio stays paused. Next: model/validate VU
dependency timing and incremental XGKICK against independent fixtures, then
rerun this captured write/read sequence.
Release and UBSan pass 2/2; Vita builds. All processes finished. Session saved.

## Return to intro rendering: shared-edge coverage

User paused audio work. Fixed triangle coverage so shared edges belong to only
one triangle (top/left inclusive, bottom/right exclusive after winding
normalization). Previously both triangles could blend the same edge pixel.
Rectangle tests cover both diagonals and windings with half-alpha blending:
every covered pixel must receive exactly one blend, with no cracks/outer spill.

The historical captured-triangle fixture changes from 122 to 106 covered pixels.
106 was independently calculated by intersecting scanlines with triangle edges,
using y in [minY,maxY) and x in [ceil(left),ceil(right)). It remains a host
reproducibility fixture, not a PS2 hardware oracle. Quarter-scale integer
coordinates still lose subpixel precision. Reference for half-open scanlines:
https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/GS/Renderers/SW/GSRasterizer.cpp

Release and UBSan pass 2/2; Vita builds. Full replay completed as
build-release/top-left-300m.txt and .ppm. Audio code is unchanged.
300000000 instructions, reason=running, PC 00272728/opcode 2443000C;
5362 VIF submissions accepted, zero rejected; 69219 triangles and 24 lines.
Framebuffer hash 2F85A16853AA9A87, 10240 nonzero RGB pixels. Converted to PNG
and inspected: gray/black rectangular regions with blue fragments, still no
recognizable intro. All processes finished. Geometry/texture and VU1 timing
remain unresolved; edge correctness is not an intro milestone.

## Repeated PRIM assembly reset

Found a direct assembly bug while researching discarded texture state:
Gif::set_prim only discarded unfinished vertices when PRIM's value changed.
A PRIM write must restart assembly even for identical mode bits. Fixed that
path for direct register writes and packed GIF tag PRE. Tests verify independent
strips do not gain connecting triangles and incomplete sprites are discarded.
Reference ApplyPRIM resets the assembly head/tail unconditionally:
https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/GS/GSState.cpp

Texture ST/Q preservation/rendering remains unimplemented; prioritized this
concrete geometry defect before adding texture diagnostics. Audio stays paused.
Full replay completed as build-release/prim-reset-300m.txt and .ppm.
At 300M, reason=running, PC 00272728/opcode 2443000C, all 5362 VIF submissions
accepted. Triangles fell from 69219 to 34810, lines from 24 to 0. Framebuffer
hash 717B270A37FF96D5; 10240 RGB pixels. Inspected converted PNG: plain dark/gray
rectangular regions; blue fragments disappeared. Still no recognizable intro.

Standalone first-vif.bin replay now emits two triangles, not five, and its
inspected image is black. Earlier thin geometry relied on cross-PRIM assembly
and must not be treated as valid intro geometry. Historical VU1 failure remains
998 pairs, PATH1 8/1, PC C0, kick 120, bad 160/tag 420F028B. No timing fix claimed.
Release and UBSan pass 2/2; Vita builds; all processes finished. Next priorities:
VU1 packet correctness and valid geometry provenance, then textured rendering.

## VU1 same-pair VF dependency fix

The functional interpreter executed upper then lower against the modified VF
register file. Added a pre-pair VF snapshot for implemented lower readers
(SQ/SQI, DIV and MTIR), so paired arithmetic cannot leak its new value into
the same pair's lower instruction. Upper/lower VF destination conflicts discard
the lower instruction, including LQI's increment, for the implemented subset.
Immediate lower payload ordering remains unchanged. This is same-pair semantics,
not full FMAC/load latency, dependency stalls or temporal XGKICK.

Tests cover paired ADD+SQI old-value storage/new-value commit, conflicting LQI
discard, and disjoint upper/lower writes. Reference dependency handling:
https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/VU1microInterp.cpp

Standalone first-vif.bin replay remains 998 pairs, PATH1 8/1, PC C0/kick120/
bad160/tag420F028B, two triangles. This defect does not resolve that captured
packet failure. No fresh 300M replay or new intro claim. Audio remains paused.
Release and UBSan pass 2/2 each; Vita builds. All processes finished.
