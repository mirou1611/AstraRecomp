# Session 2026-09-08

Resumed from September 7 handoff (`be79c37`) on
`research/spu2-dma-checkpoint`. User research files remain untouched/untracked.

## VU0 outer-product checkpoint

The saved stop at EE PC 002738F8, opcode 4BC522FE, is VOPMULA.
Added explicit VU0 accumulator storage, cleared by CPU reset and FBRST VU0
reset. Implemented functional VOPMULA and VOPMSUB, with all XYZ results
calculated before destination writes to handle source/destination aliases.
Both instructions use the same cyclic operand order; the captured BIOS
VOPMSUB swaps FS/FT to form the other half of the cross product.

Reference: [PCSX2 VU arithmetic implementation](https://github.com/PCSX2/pcsx2/blob/master/pcsx2/VUops.cpp).
The reference check corrected an initial reversed-lane draft before the full
integration replay. Regression tests cover the captured pair, both aliases,
VF0, W preservation, unchanged ACC after VOPMSUB, same-operand cancellation,
and CPU/FBRST resets. Host CTest passes 2/2; Vita cross-build passes.

This remains a functional subset: host floating-point arithmetic is not a
complete model of PS2 exceptional values, MAC/status flags, or VU pipeline
timing. No intro/audio/performance claim follows from instruction coverage.

VPK SHA-256:
`7D10B24337C7D9D35742056327E09A17DB78C360F56C19CE93E03319CF7EC1A9`.

Full BIOS replay (`build-release/outer-product-300m.txt`) stopped at step
250509266, PC 002737E8, opcode 4BE5233C (VMOVE), 71 instructions beyond
the saved frontier. Framebuffer remains black, hash AE6BFFA9F3064B83;
VIF rejects 9/10 packets, first unsupported AE13000C, and VU1 remains at
998 pairs. Code and initial notes were pushed as 69934a1.

## VMOVE and matrix accumulator sequence

Added bit-preserving masked VMOVE with self-alias/VF0 tests. The captured
matrix code also contains VMULAbc, VMADDAbc, and VMADDbc; implemented these
functional broadcast operations against explicit ACC storage. Scalar inputs
are snapshotted before any writes. Tests run the captured four-column
transform and check all broadcasts, masks, aliasing, unchanged ACC for VMADD,
and zero destination. Host tests pass 2/2 and the Vita build passes.

Reference: [PCSX2 v2.4.0 VU semantics](https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/VUops.cpp),
with COP2 encodings checked against the existing local PCSX2 opcode reference.
The same FP/flag/timing limitations noted above apply.

VPK SHA-256:
`C9B5D70679A8972492F6DFA248BE461E4EDBFE2C64C1FCFD9F055E54C482E30B`.
Integration replay `build-release/matrix-acc-300m.txt` stops at step 250516994,
PC 0021CCD0, opcode 72968000 (EE MADD), 7,728 instructions beyond VMOVE.
VIF now rejects 14/15 submissions; first unsupported code and black framebuffer
hash are unchanged. VU1 remains at 998 pairs. Checkpoint pushed as 2f1ed57.

## EE MADD and VIF failure location

Implemented MADD/MADDU/MADD1/MADDU1 with modulo-64-bit accumulation using
unsigned addition (no signed-overflow UB), low-word source operands, and
separately sign-extended HI/LO word results. Tests cover both banks,
signed/unsigned products, carry and wraparound, source aliases, zero
destination, preserved GPR upper half, and untouched alternate accumulator.
Reference: [PCSX2 MMI arithmetic](https://github.com/PCSX2/pcsx2/blob/master/pcsx2/MMI.cpp).

VIF diagnostics now retain the first unsupported command's one-based packet
number, byte offset and submission size. These are fixed-size metadata, not
a full capture or proof of byte provenance. Tests verify sticky first-failure
location, reset, and that truncated input does not fabricate an unsupported
command. The trace tool prints this metadata. Host tests pass 2/2; Vita build
passes. Pending integration outputs: `build-release/vif-location-300m.txt`
(before MADD) and `build-release/madd-300m.txt` (with MADD).

VPK SHA-256:
`9C2427DE5F7FF209F6A35FE9B5E58B31EC7A8273002D35C8728B4CCA6B4A8867`.

MADD integration stops at step 250533991, PC 002730AC, opcode 4A0002FF
(VNOP), advancing another 16,997 instructions. VIF rejects 31/32 submissions;
framebuffer remains black and VU1 stays at 998 pairs. Both pre-MADD and MADD
diagnostic runs identify unsupported AE13000C at offset 0 of submission 2,
which is only 16 bytes. This is a separate submission from the first captured
3,936-byte VIF packet. Investigate its DMA source/framing rather than assuming
it is caused by the earlier rejected PATH1 tag.

Checkpoint f7e8975 is pushed. A separate Debug build with
`-fsanitize=undefined -fno-sanitize-recover=undefined` passes CTest 2/2.

Added VNOP with a regression preserving ACC/status while advancing PC.
The trace tool now reports latest completed DMA spans at the first unsupported
VIF event, explicitly labeled as latest-DMA metadata rather than guaranteed
queued-packet provenance. Host tests pass 2/2; Vita build passes.
Replay pending: `build-release/vnop-source-300m.txt`.
VPK SHA-256:
`A47663369563B17246078AB620C084CFA7FC961597F2D7362C30D99F642C15D5`.

VNOP/source replay stops at step 250534106, PC 0027371C, opcode 4BE5217D.
The first VIF failure occurs at step 249419953, next EE PC 0021858C;
latest completed DMA spans are source 00002298 (8 bytes) and 02202828
(8 bytes), final TADR 02202830. Graphics remain unchanged. This is consistent
with a bad DMA address interpretation but does not itself identify the writer.

## VIF DMA scratchpad addressing

Source inspection found that the VIF chain walker discarded the SPR selector
in TADR and tag addresses. Three synthetic tests failed before the fix:
scratchpad-resident tags, a RAM REFE referencing scratchpad, and inline data
wrapping at the 16 KiB boundary. Preserve encoded DMA addresses for flow and
register writeback, translate SPR accesses to scratchpad, and wrap byte reads.
Source-span diagnostics use CPU-visible addresses and split at scratchpad wraps.
Added a mixed RAM NEXT-to-scratchpad chain with TTE to check transition and
tag-payload ordering. All host tests pass 2/2 and Vita cross-build passes.

Reference: [PCSX2 DMA address selection](https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/Dmac.cpp).
The fix is scoped to VIF1 chain reads; other DMA paths and complete timing,
backpressure, bus errors, and snapshot semantics are not implied correct.

VPK SHA-256:
`594A516D4B439949AA8036D82883A8380FA82147E695ACFBDAEFCE92C469E089`.
Fixed BIOS replay in `build-release/vif-scratchpad-300m.txt` confirms an
integration effect: submission 2 is now 80 bytes, with source spans
70002298 (8 bytes), 700022A0 (64 bytes), and 700022E8 (8 bytes).
First unsupported command is now 50000002 at byte offset 4 (VIF DIRECT),
instead of the bogus AE13000C command from low RAM. Failure occurs at step
249419977, next EE PC 002171C4, TADR/MADR 800022F0.

CPU stopping point remains step 250534106, PC 0027371C, opcode 4BE5217D.
VIF rejects 31/32 submissions and the framebuffer remains black, hash
AE6BFFA9F3064B83; VU1 stays at 998 pairs and five triangles. Thus this fixes
transport, not rendering or the intro. Scratchpad checkpoint pushed as 53a6f8e.
Release and UBSan suites pass 2/2; isolated first-VIF replay under UBSan still
reproduces 998 pairs, 8/1 PATH1 tags and five triangles without sanitizer errors
(tool exit 1 reflects its known rejected PATH1 chain).

## Next handoff

1. Implement/test VIF DIRECT forwarding to GIF, starting with captured command
   50000002 (two qwords). Establish payload sizing, ordering, truncation handling,
   and any alignment requirements from primary references; do not simply skip
   the command or mark it accepted without delivering its payload.
2. Separately verify and handle EE macro opcode 4BE5217D at 0027371C.
3. Preserve the first captured PATH1 rejection as an independent regression;
   temporal XGKICK remains unresolved.
4. Run release tests, Vita build, and a full integration replay after the next
   coherent checkpoint; record observed results rather than a boot percentage.

No code/build/replay processes remain running at this handoff. User research
files and firmware remain local and untouched; no physical Vita result claimed.

## VIF DIRECT forwarding

Implemented DIRECT (0x50) payload forwarding to GIF through a VIF-owned output
queue, preserving functional order with earlier VU1 PATH1 output. Immediate
QWC zero means 65536 qwords. Payloads may span submissions; incomplete DIRECT
data is buffered (at most 1 MiB per command), not misdecoded as VIF opcodes or
silently discarded. Reset clears pending data/output. DIRECTHL and actual GIF
arbitration, backpressure, and streaming timing remain unsupported.

Reference: [PCSX2 VIF DIRECT handling](https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/Vif_Codes.cpp).
Tests cover payload splits, command ordering, zero count, reset, PATH1 before
DIRECT, and a real DMA-to-GS pixel result. Host release and UBSan tests pass
2/2; Vita cross-build passes. The standalone replay drains the same output
queue and reports incomplete DIRECT data as unsuccessful at end of input.

Full replay `build-release/vif-direct-300m.txt` now accepts all 32 VIF
submissions (zero rejected), and GIF accepts all 68 submitted packets.
GIF reports 10 sprites, with five triangles and 998 VU1 pairs;
the original rejected PATH1 tag remains recorded (8/1). Framebuffer now has
5040 nonzero RGB pixels, hash B85955B0A95C6083, instead of black. CPU still
stops at step 250534106, PC 0027371C, opcode 4BE5217D.

Visual inspection completed: the second replay captured `build-release/vif-direct.ppm`,
converted losslessly to PNG for viewing. It shows a gray rectangle in the upper
right against black, not a recognizable PS2 intro. The repeated replay preserves
hash B85955B0A95C6083 and reports zero pending DIRECT bytes.
The trace tool accepts '-' for census path to request an image without census
 overhead. Do not describe this rectangle as the PS2 intro.
VPK SHA-256:
`1312C8C889F31797EC4FCAC527BD1542C16E04BE6D68952FA2565A938E9486D7`.

## VFTOI and VABS checkpoint

The captured opcode 4BE5217D decodes to VFTOI4 (special2 0x15), not VABS;
an initial label/test encoding was corrected by the opcode table and failing
regression before integration. Added VFTOI0/4/12/15 using scale, truncation
toward zero, and saturation before integer conversion to avoid out-of-range
cast UB. Separately added VABS (special2 0x1D) as bitwise sign clearing.

Tests cover the captured VFTOI4 encoding, all scaling variants, aliases, VF0,
signed zero, finite integer boundary, overflow/exceptional bit patterns and
masked W; VABS covers masks and preservation of non-sign bits and STATUS.
Release and UBSan CTest both pass 2/2, and Vita cross-build passes. These do
not establish complete PS2 floating-point semantics or pipeline accuracy.
Primary reference is the VUops.cpp source linked above.

VPK SHA-256:
`95B92C515E0F97D575C1C5444C034DF74D4DE404C2E103FBB9E968F1C3DFE197`.
Replay completed: `build-release/vftoi-300m.txt`, image `build-release/vftoi.ppm`.
Stops at step 252572171, PC 002736B4, opcode 4A2701C4, advancing 2,038,065
instructions beyond VFTOI4. All 160 VIF submissions accepted, all 196 GIF
packets accepted, and 6275 triangles emitted. VU1 remains at 998 pairs with
the earlier 8/1 PATH1 tag record: the new triangles do not prove that VU1
timing issue was fixed. Pending DIRECT bytes are zero.

Framebuffer hash 301EB407483F092F, 9664/17920 nonzero RGB pixels. Converted
the PPM to PNG and inspected it: gray polygon shapes against black, not a
recognizable PS2 intro. This is a substantial increase in exercised graphics
work, not visual correctness or physical-Vita performance proof.

Latest code checkpoint 92b767a is pushed. Resume at opcode 4A2701C4
(verify broadcast subtract semantics against the opcode table/source), then
continue full replay and image inspection. DIRECT is now exercised successfully
at this frontier; do not restart diagnosis at the already-fixed AE13000C or
50000002 failures. Rendering fidelity, original PATH1 timing, audio and real
Vita measurements remain outstanding. All current build/replay processes have
completed; working code is committed and user research files remain untouched.

## Audio focus and VSUB broadcast

User explicitly requested continued work with audio focus. Read game-audio
skill; applied its platform-validation emphasis without substituting designed
or placeholder sounds for emulated PS2 audio.

Audio audit: existing runtime implements SPU2 register storage, DMA transfers,
and sample RAM, but has no voice sample decoder/mixer or Vita audio output.
Added a standalone SPU2 ADPCM block decoder as the first playback foundation:
16 bytes to 28 signed PCM samples, low nibble first, five supported predictors,
shift handling, clipping, and per-voice history retained across blocks.
Flags are returned for future voice loop/end handling. Unsupported predictor
IDs fail without mutating history/output. No playback is connected yet.

Reference: [PCSX2 SPU2 block decoder](https://github.com/PCSX2/pcsx2/blob/v2.4.0/pcsx2/SPU2/Mixer.cpp).
Arithmetic avoids signed left-shift UB and implementation-defined negative
right shifts. Tests cover predictor golden values, all shifts, clipping,
consecutive blocks, flags, and failure atomicity. Voice addressing, key-on/off,
ADSR, pitch/interpolation, mixing, reverb, and device output are still required.

Added VSUBx/y/z/w for captured opcode 4A2701C4, with captured-mask and
aliasing regressions. The trace tool now summarizes nonzero IOP key-on store
attempts and union of requested voice bits per core (not actual playback).
Release and UBSan tests pass 2/2; Vita build passes. Full replay pending.
Replay: `build-release/vsub-audio-300m.txt`, image `build-release/vsub-audio.ppm`.

VPK SHA-256:
`75A9A02DA2608D2F8F36A5108B0DD58BCDC20BD789A93D956BC3C9D44F05EFA9`.
