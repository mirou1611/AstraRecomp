# Session checkpoint — 2026-09-15

## Reference emulator installed locally; configuration check passes

Continuing from 77f69fb on research/spu2-dma-checkpoint. Main goal remains a
recognizable PS2 intro; audio and physical Vita measurements remain deferred.

Downloaded the official PCSX2 v2.8.2 Windows portable archive from:
https://github.com/PCSX2/pcsx2/releases/tag/v2.8.2

Archive: pcsx2-v2.8.2-windows-x64-Qt.7z, 25,670,075 bytes.
SHA-256 verified against GitHub release asset metadata:
`7dfc829ca1994cc1045ac49f05e39b6cf968b72e6a374c40e05c2a2b4ac200b4`

Extracted successfully into ignored `.tools/pcsx2-v2.8.2`. The executable's
`-help` runs and identifies v2.8.2. Nothing was installed globally.

## Startup failure diagnosed and resolved

The initial `-datapath .../.tools/pcsx2-reference-data -testconfig` probe did
not exit or produce a profile. Source inspection showed that this release
appends `PCSX2` to the custom path and uses nonrecursive directory creation.
The supplied parent directory did not exist. After explicitly creating that
parent, the same command exited successfully and created:

`.tools/pcsx2-reference-data/PCSX2/inis/PCSX2.ini`

Source checked:
https://raw.githubusercontent.com/PCSX2/pcsx2/v2.8.2/pcsx2/Pcsx2Config.cpp
(`EmuFolders::SetDataDirectory`, especially custom-path and creation handling).
The stalled process launched for the failed probe was stopped by its exact PID
and verified executable path. No user PCSX2 process/profile was intentionally
modified. Computer-use inspection was attempted according to its skill, but
the native pipe was unavailable; no UI action was performed and no dialog was
observed. Do not claim a particular dialog caused the stall.

## Next action, not yet performed

The generated profile still has `SetupWizardIncomplete=true`; BIOS selection
and renderer configuration have NOT been completed. No BIOS or fixture has
been booted in PCSX2, and no reference frame or GS dump exists yet.

Configure only this isolated profile using verified setting names, point it
at the already-local BIOS without distributing it, and select a reproducible
software-renderer capture route. The fixture's display/timing setup remains
incomplete; consult SESSION_2026-09-14.md before treating its ELF as a portable
PS2 demo. GS dumps require initial state restoration, not just GIF bytes.

No emulation code changed in this checkpoint. Existing latest validation:
Release CTest 3/3 and UBSan CTest 3/3 at 77f69fb. Those tests were not rerun for
this tools-only setup. Downloaded binaries, profiles and BIOS stay uncommitted.
