# Session checkpoint — 2026-09-15

## Reference emulator installed locally; configuration check passes

Continuing from 77f69fb on research/spu2-dma-checkpoint. Main goal remains a
recognizable PS2 intro; audio and physical Vita measurements remain deferred.

Downloaded the official PCSX2 v2.8.2 Windows portable archive from:
https://github.com/PCSX2/pcsx2/releases/tag/v2.8.2

Archive: pcsx2-v2.8.2-windows-x64-Qt.7z, 25,670,075 bytes.
SHA-256 verified against GitHub release asset metadata:
`7dfc829ca1994cc1045ac49f05e39b6cf968b72e6a374c40e05c2a2b4ac200b4`

Extracted successfully into ignored `.tools/pcsx2-v2.8.2`. The executable's
`-help` runs and identifies v2.8.2. Nothing was installed globally.

## Startup failure diagnosed and resolved

The initial `-datapath .../.tools/pcsx2-reference-data -testconfig` probe did
not exit or produce a profile. Source inspection showed that this release
appends `PCSX2` to the custom path and uses nonrecursive directory creation.
The supplied parent directory did not exist. After explicitly creating that
parent, the same command exited successfully and created:

`.tools/pcsx2-reference-data/PCSX2/inis/PCSX2.ini`

Source checked:
https://raw.githubusercontent.com/PCSX2/pcsx2/v2.8.2/pcsx2/Pcsx2Config.cpp
(`EmuFolders::SetDataDirectory`, especially custom-path and creation handling).
The stalled process launched for the failed probe was stopped by its exact PID
and verified executable path. No user PCSX2 process/profile was intentionally
modified. Computer-use inspection was attempted according to its skill, but
the native pipe was unavailable; no UI action was performed and no dialog was
observed. Do not claim a particular dialog caused the stall.

## Next action, not yet performed

The generated profile still has `SetupWizardIncomplete=true`; BIOS selection
and renderer configuration have NOT been completed. No BIOS or fixture has
been booted in PCSX2, and no reference frame or GS dump exists yet.

Configure only this isolated profile using verified setting names, point it
at the already-local BIOS without distributing it, and select a reproducible
software-renderer capture route. The fixture's display/timing setup remains
incomplete; consult SESSION_2026-09-14.md before treating its ELF as a portable
PS2 demo. GS dumps require initial state restoration, not just GIF bytes.

No emulation code changed in this checkpoint. Existing latest validation:
Release CTest 3/3 and UBSan CTest 3/3 at 77f69fb. Those tests were not rerun for
this tools-only setup. Downloaded binaries, profiles and BIOS stay uncommitted.

## First reference ELF execution and per-draw RGB comparison

Configured isolated BIOS selection, software renderer (Renderer=13), muted
output, DumpGSData/SaveRT/SaveFrame/SaveInfo and bounded dump ranges. Both
HWDumpDirectory AND SWDumpDirectory must be set: Pcsx2Config.cpp disables dumping
if either is empty. Captures are under `.tools/pcsx2-reference-data/draws`.
The BIOS was copied to the isolated profile's bios directory after PCSX2 created
a .mec alongside the original; that generated sidecar was moved into the profile.
No firmware or sidecar is committed.

A relative ELF path failed during BIOS loading. An absolute Windows path to
`.tools/pcsx2-reference-data/path3-loop.elf` works: the log confirms execution
at 00100000. Each bounded reference process was stopped by its exact PID and
verified executable path. No reference process was intentionally left running.

Without display scanout the last triangle remained buffered: the first dump
contained only the clear sprite. FINISH did not produce the triangle capture.
Replaced that experiment with an off-scissor point after a primitive-class
change, which flushes the preceding triangle batch. This is fixture-only code;
it does not alter Astra's emulation behavior. PCSX2 vertex dump explicitly
reports `flush_reason: CONTEXT CHANGE` and vertices (0,0), (128,0), (0,128),
with UV (0,0), (2,0), (0,2).

Reference ELF SHA-256:
`49b13e13efa43df65089e0af2268b5a183c2e337ac4a9f0e5bec32b7dac216a8`
Reference triangle: `00002_f00010_rt1_00400_C_32.png`, a 129x129 RT crop.
Visually inspected: red/green/blue triangle on black. Exact RGB comparison over
the defined 128x128 region: **0 / 16,384 mismatches**. At coordinates (4x,4y)
matching Astra's quarter-scale grid: **0 / 1,024 mismatches**. The extra crop
border and alpha were not compared. Reproduce with:
`powershell -File tools/check_path3_reference.ps1 -ImagePath .tools/pcsx2-reference-data/draws/00002_f00010_rt1_00400_C_32.png`

This is the first PCSX2-backed check of the simple fixture, not hardware truth,
not a full-frame BIOS comparison, and not a GS-dump importer. Privileged display
setup is still absent. Next expand reference fixtures to perspective ST/Q and
other rendering gaps, using per-draw comparisons rather than another blind
800M replay. Final Release and UBSan CTest both pass 3/3 after the fixture change.

## Perspective ST/Q reference and implementation

Added --stq to the ELF fixture and a separate path3_stq_graphics_tests CTest
target. The vertices use (S,T,Q)=(0,0,1), (2,0,2), (0,1,1). For native screen
coordinates within the 128x128 triangle, texel boundaries follow 3*x>=128 and
2*y>=128+x. PCSX2 SW draw 2 matched this independent RGB formula exactly:
0/16384 native mismatches and 0/1024 quarter-grid mismatches. Reference outputs
are isolated in `.tools/pcsx2-reference-data/stq-draws`; verifier accepts
`-Perspective`. Reference ELF SHA-256:
`78ce38242ba36cff95ffa37e3cea57c109ab5818726c66bdde83f913ffeacf50`

Before implementation Astra failed all 528 covered pixels. Implemented affine
interpolation of S, T and Q followed by S/Q and T/Q, scaled by TEX0 dimensions.
The original fixed-UV path remains. The existing sampler and GS output tests
handle texel/color and alpha/depth/blend stages. The 3d-games skill informed this
stage separation; PS2-specific correctness is checked with the reference fixture.

This is a finite, nonnegative-coordinate subset using the current unsigned
logical-memory sampler. Zero/nonfinite interpolated Q, nonfinite/negative or
unsigned-out-of-range texel coordinates skip the fragment to avoid undefined
C++ float-to-integer conversion. This is a safety policy, NOT a hardware-verified
PS2 edge-case rule. CLAMP/repeat semantics, texture formats/swizzle, TFX/TCC,
filtering and subpixel coverage remain incomplete. No general GS accuracy claim.

Tests cover the changing-Q texture, independent Q writes, reverse winding,
texture dimensions and invalid-coordinate safety. Release CTest 4/4 passes and
UBSan CTest 4/4 passes; Vita VPK builds. Both fixture images were visually
inspected. A 300M BIOS replay was started to compare actual boot output;
do not report an image or endpoint until that run finishes.

## Long replay interrupted; bounded host-time diagnostics added

The ST/Q 300M replay did NOT complete. After more than 38 minutes of CPU time
without an endpoint/PPM, it was interrupted. Last complete logged audio probe
was around 179.8M EE instructions; buffered stdout is not an exact stopping PC.
The cause of the slow run is not established. Do not attribute it to the new
renderer or claim a BIOS visual improvement. Earlier intermediate commentary
about reaching first-VIF activity was not a reliable completion observation.

Added optional ASTRA_TRACE_SECONDS=1..86400 to ps2bios_trace. It checks a steady
host clock every 65,536 EE steps, prints/flushed progress to stderr at roughly
five-second intervals and exits the main loop at the host deadline, preserving
normal diagnostic/framebuffer output. host_deadline is reported separately from
the guest stop reason. It cannot interrupt a single stuck instruction/device
operation; an external process bound is still needed for that case. Default 0
or unset leaves the old behavior unchanged. This changes host tooling only.

Five-second smoke run stopped at 5,373,952 steps, PC8000E5E8, with
host_deadline=1, guest reason=running and a valid black framebuffer snapshot
(hash AE6BFFA9F3064B83). This is early boot, not an intro result. Next use this
bounded progress mode to locate the long-run slowdown before another lengthy
BIOS comparison. Audio implementation remains out of scope.

## Bounded ST/Q BIOS comparison completed

The follow-up replay completed all 300,000,000 EE instructions in approximately
276 seconds, below its 360-second host deadline (`host_deadline=0`). Progress
remained steady through the previously suspicious 179.8M region. This does not
reproduce or explain the earlier interrupted 38-minute run; no speculative
performance fix was applied. Command, from the WSL workspace directory:

```sh
ASTRA_TRACE_SECONDS=360 ./build-release/ps2bios_trace ps2-0200e-20040614-100914.bin 0 300000000 1 0 8 0 0 0 - build-release/stq-bounded-300m.ppm > build-release/stq-bounded-300m.txt 2>&1
```

Endpoint: guest reason `running`, PC00272728, opcode2443000C; IOP37,500,000
cycles, PC0000AE94. GIF5418 packets, zero rejected, 191 sprites, 34810 triangles,
tags5405/0/13, image_bytes368640. These EE endpoint and graphics counters match
the previous `incremental-kick-300m` replay. VIF5362 packets, zero rejected;
VU1 remains at 998 pairs, stopped at0308, with PATH1 tags8/1 and first rejected
tag00000000420F028B at0160. ST/Q has not resolved that separate rejection.

Framebuffer hash changed from717B270A37FF96D5 to832E075FD7D55314; the new image
contains10028 nonblack RGB pixels. Converted and visually inspected the PPM:
dark/gray rectangular areas now contain repeated texture detail, with a black
lower region. **Still no recognizable PS2 intro.** A changed image is not proof
that BIOS rendering is correct; only the small ST/Q fixture has a pixel oracle.
The local log and images remain ignored, and no firmware is committed.

Next: extend small reference-backed texture tests before changing BIOS rendering
again. Inspection confirms `sample_texture` currently ignores CLAMP and treats
all nonzero TFX values alike; TCC handling is also incomplete. These are concrete
coverage gaps, not yet demonstrated causes of the boot image. Preserve the
accurate fallback and do not add arbitrary XGKICK timing delays. Audio and
physical Vita testing remain deferred.

Verification after the completed replay: Release CTest4/4 and UBSan CTest4/4
pass. No emulation code changed in this follow-up; this checkpoint records the
completed experiment and the next bounded correctness target.

## TEX0 RGB alpha selection regression fixed

Added PATH3 ELF variants `--rgb-decal` and `--rgb-modulate`. Both use vertex
alpha0x40, texture alpha0xFF, and EQUAL0x40/KEEP alpha testing after the clear.
This makes incorrect alpha selection visible even in RGB-only reference dumps.
Before the fix each variant failed all528 covered host pixels. PCSX2v2.8.2 SW
rendered both as expected: zero mismatches over16384 native RGB pixels and1024
quarter-grid samples using `tools/check_path3_reference.ps1` without switches.
Local reference draws are in `.tools/pcsx2-reference-data/rgb-decal-draws` and
`rgb-modulate-draws`, each `00002_f00010_rt1_00400_C_32.png`.

Reference persistent ELF SHA-256:
- DECAL: D61E8E7FBABECA97B8F8ACF7BA783133884DDE4502BAF38E3E6FC6C9C2578116
- MODULATE: 5B94C5C8A522C2AC6107100D2D45F8AB459E3C999F236637C6058EBC7B5C0234

The sampler now preserves vertex alpha when TEX0.TCC=RGB, before alpha test
and blending. TCC=RGBA retains existing texture-alpha behavior. Unit coverage
checks both contexts, both functions, both TCC modes, and four vertex alpha
values, with accepting and rejecting alpha tests. Non-neutral RGB checks also
exercise modulation and saturation. An older scissor fixture expected texture
alpha but omitted TCC=RGBA; corrected its TEX0 setup, retaining its expected
pixels. The 3d-games skill informed keeping fragment color selection separate
from output tests. The installed PCSX2 shader's `tfx` function also corroborates
the TCC selection, but executable reference comparisons are the primary oracle.

This does not implement HIGHLIGHT/HIGHLIGHT2 RGB formulas, texture CLAMP, or
general GS accuracy. No BIOS replay after this change yet; no new intro claim.
Both launched reference processes were stopped after capture. Firmware and
reference captures remain local and ignored.

Verification: Release CTest6/6, UBSan CTest6/6, Vita VPK build pass. Both new
fixture exports report zero host pixel mismatches after the fix. Host DECAL
image and reference DECAL draw were visually inspected. Next bounded target:
reference-backed HIGHLIGHT/HIGHLIGHT2 formulas or texture addressing; do not
interpret the corrected alpha path as proof of BIOS intro completion.

## HIGHLIGHT and HIGHLIGHT2 reference milestone

Implemented the two remaining texture color functions: both add vertex alpha
to modulated RGB before saturation; RGBA HIGHLIGHT adds texture and vertex
alpha, while RGBA HIGHLIGHT2 selects texture alpha. RGB mode preserves vertex
alpha. Expanded unit coverage to all four functions, both contexts/TCC values,
and alpha0/64/128/255, including saturation and rejecting alpha tests.

New PATH3 variants `--highlight` and `--highlight2` use texture alpha0x20,
vertex alpha0x40, and EQUAL alpha tests of0x60 and0x20 respectively. Each failed
all528 covered pixels before implementation and passes afterward. PCSX2v2.8.2
SW captures match independently specified RGB at all16384 native pixels and
all1024 quarter-grid samples (`check_path3_reference.ps1 -Highlight`). Captures
remain in ignored `highlight-draws` and `highlight2-draws` reference folders;
both launched reference processes were stopped after capture.

Persistent ELF SHA-256:
- HIGHLIGHT: 38BFFA1CB459C3D2AB87155762318F29DBFCC3C468AF7731B100E71EECB129F0
- HIGHLIGHT2: 9245BF9364978B7F1CB539FEB6F3670ECC41A2BA787159B837BAE72CCE292EF9

Release CTest8/8 and UBSan CTest8/8 pass; Vita VPK builds. The 3d-games skill
informed isolating texture color processing from output tests. These small
reference comparisons do not establish general GS accuracy or intro completion.
Next run the bounded300M BIOS comparison with the TCC and HIGHLIGHT fixes.
