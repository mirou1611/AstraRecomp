# Session 2026-09-20: BIOS render-to-texture evidence

## Completed capture, not intro completion

The instrumented300M replay completed at PC00272728, opcode2443000C,
host_deadline=0 (420-second guard). Output: build-release/draw-state-300m.txt
and .ppm. The PPM is byte-identical to addressing-300m.ppm; framebuffer hash
832E075FD7D55314. No visual improvement is claimed.

The old64-triangle prefix contains only host-space zero-area triangles. Added
a separate bounded64-record nonzero-host-area selection, retaining the original
prefix and absolute zero-based triangle sequence. Records now include TEX0,
CLAMP, FRAME, ALPHA and printed per-vertex ST/UV/Q. FRAME registers are retained
for diagnostics only: this does NOT implement framebuffer routing. Tracing is
off by default. Regression checks cover disabled capture, independent bounds,
survival of70 degenerate draws, context isolation and reset. The 3d-games skill
informed separating geometry/texture inputs from output routing.

In this BIOS run the first selected triangle is sequence3075. Of64 selected
records,24 target FRAME00000000000A0000 and40 target00000000000A0050.
Several early selected triangles have wrapped/extreme coordinates; selection
is not a visibility test or proof that geometry is correct.

## Actual PCSX2 BIOS capture

Used installed PCSX2v2.8.2 software renderer with the isolated local profile,
same local BIOS, -bios -batch -nogui -nofullscreen. CLI behavior verified from:
https://raw.githubusercontent.com/PCSX2/pcsx2/v2.8.2/pcsx2-qt/QtHost.cpp

Saved bounded first100 draw captures with textures and context/vertex info to
.tools/pcsx2-reference-data/bios-first-draws. Reference process8856 was stopped
after capture, with executable path checked. No firmware/captures are committed.
Reference configuration now points at that folder and has SaveTexture=true.

Draw98 (reference frame60) writes FRAME base0, width640, PSMCT32. Draw99 samples
TEX0 base0, width640, PSMCT24, dimensions1024x256 and writes FRAME base0x1e00
in the reference's printed256-byte units. It is a screen-covering fixed-UV
sprite. TEXA has AEM=1, TA0=127, TA1=0; linear filtering is enabled.

Compared RGB of draw98's482x146 render-target crop with the corresponding
origin crop of draw99's1024x256 raw input texture: zero mismatches across70372
pixels. Files:
- 00098_f00060_rt1_00000_C_32.png
- 00099_f00060_itexraw_00000_C_24.png
- 00099_context.txt / 00099_vertex.txt

Visually inspected draw99 output: recognizable blue/cloud boot imagery is
present in the REFERENCE, not Astra. This demonstrates an actual BIOS
render-to-texture dependency, not merely an inferred feature wishlist.

## Concrete implementation gap / next target

Astra's Gs::write writes a standalone160x112 color vector, while Gif texture
sampling reads its separate4MB upload buffer. FRAME previously was ignored
and is now only recorded. PSMCT24 texture sampling is unsupported (falls back
to vertex color); TEXA and linear filtering are also unimplemented. Therefore
the demonstrated reference feedback draw cannot be reproduced by this model.

Do not implement a special-case framebuffer screenshot copy for this BIOS.
Next construct a small PATH3 two-target fixture: draw known colors into targetA,
sampleA as PSMCT24 while drawing targetB, then verify target isolation, RGB and
TEXA-derived alpha against the reference. Define shared color storage/addressing
and output selection explicitly before integrating it with the raster backend.
Account for the host quarter-resolution approximation and existing linear
texture-storage limitations; do not claim full native VRAM correctness.

This is not a lockstep comparison: PCSX2 draw98/99 are NOT proven equivalent to
an Astra sequence number, and the capture does not rule out CPU/VU geometry or
timing bugs. Render-to-texture is a proven missing capability used by the intro,
not a proven sole cause of the missing intro.

Verification: Release CTest13/13, UBSan CTest13/13, Vita VPK build pass.
The instrumented BIOS frame remains byte-identical; diagnostic changes did not
alter this replay's rendered output. No physical Vita test was performed.

## End-of-day handoff (user requested stop)

Stopped after checkpoint c165481, pushed to origin/main. No further implementation
started. Reference PCSX2 process is no longer running. The300M replay completed.
User-owned untracked research plan, Coworker1337.txt and coworker2 remain untouched.

Resume with the two-target PATH3 fixture described above, then implement and
reference-check the shared framebuffer/texture path. Do not repeat the unchanged
300M replay before a relevant behavior change. Intro remains unachieved; audio
and physical Vita testing remain deferred. All detailed evidence and local
capture locations needed to continue are recorded in this session file.
