# AstraRecomp session handoff — 2026-08-30

## Milestone reached

AstraRT now validates generated package metadata before executing native code.
The package contract includes:

- AstraRT ABI version;
- guest address bounds;
- a lowercase SHA-256 source fingerprint;
- a non-empty, strictly sorted function table;
- aligned, non-empty, non-overlapping entry ranges;
- non-null translated function pointers.

Malformed packages return an `Interpreter` exit without invoking translated code.
Bounded dispatch validates once before entering its function loop, so validation
does not add an O(entry-count) scan to every native call or return.

The Phase-0 caller range was split at its resumable `0x3008` entry. This makes
the generated metadata non-overlapping while preserving exact-entry lookup and
the existing JAL/delay-slot/`jr $ra` behavior.

## Verification

```text
Host build: passed
ps2core_tests: 100% passed, 0 failed
VitaSDK cross-build: passed
VPK: build-vita/ps2vita.vpk
SHA-256: FEACD59090C1FC80C0DB735455F7F530269E8E0621D227A836FCACA4075136EF
```

New host coverage rejects unsorted tables, overlapping ranges, unsupported ABI
versions, and malformed fingerprints, and verifies safe interpreter fallback.

## Automatic backend milestone

The first deterministic ELF-to-AstraRT generator is now integrated into CMake.
It emits the validated package, aggregate source fingerprint, exact basic-block
table, and compact native functions during both host and Vita builds. The manual
Phase-0 translated bodies were removed from `src/core/aot.cpp`.

Supported slice:

- `ADDIU`, `ADDU`, `LW`, and `SW`;
- `J`, `JAL`, `JR`, `BEQ`, and `BNE` with data/NOP delay slots;
- direct, indirect, terminal, and interpreter exits;
- BREAK with exact PC and cycle accounting;
- unsupported-opcode fallback without consuming the instruction.

Six redistributable corpora produce 14 sorted, non-overlapping block entries.
Seeded differential coverage checks 32 arithmetic/load/store inputs, both paths
of BEQ and BNE, converging direct jumps, JAL/JR, and unsupported ORI fallback.
A second generation pass compared byte-for-byte equal with the build output.

Final verification after the generator milestone:

```text
Host build: passed
ps2core_tests: 100% passed, 0 failed
VitaSDK cross-build: passed
Generated package fingerprint: 71bb69a814f59b8d3ab3a3bdf1586b0d1438a455238bb88d5cd8dbbcc133fb44
VPK SHA-256: 944AF84EB6CD8A3A193E0577398F49B730791A10DB83727FBCF14343E1D149DB
```

Vita3K installed the final VPK as a 138,125-byte `eboot.bin` with the current
build timestamp. The generated package ran visibly while BIOS execution
continued, showing:

```text
PS2R AOT T0: PASS   INTERP:42   NATIVE:42
PS2R AOT T1 CALL/RET: PASS   NATIVE:42
```

Visible evidence:

```text
C:\Users\amir\AppData\Local\Temp\astrarecomp-generated-vita3k-clean.png
```

Generated LW/SW now check alignment and `Memory::valid` before access. Invalid
accesses commit preceding native instructions and return an interpreter exit at
the exact faulting PC without consuming or touching memory. Host coverage proves
a misaligned SW yields at `0x4004` after the preceding ADDU. Memory operations
are deliberately excluded from generated delay slots until AstraRT can preserve
pending-branch exception state.

## Next engineering target

Expand the generator fuzz corpus, then return to the BIOS path: capture the
current MMIO synchronization loop and identify the missing IOP/SIF/interrupt
event blocking the first GIF traffic.
