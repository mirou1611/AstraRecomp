# Session checkpoint — 2026-09-10

## Main goal and scope

Continue toward a recognizable PS2 intro on Vita. Audio remains paused by user
request. Research code stays on `research/spu2-dma-checkpoint`.

## VU1 pending division at program termination

Fixed pending Q retirement after the E-bit delay pair completes. Previously a
microprogram could stop with stale Q, leaving its division pending until a later
execution step. Host instruction-budget exhaustion still does not drain Q.
Program-end drain cycles are included in the existing Q stall counter. This is
only the modeled division pipeline, not a complete hardware pipeline flush.

Regression tests cover DIV in the E-marked pair and DIV in its delay pair,
single-pair host budgets, final Q/cycle/pair counts, and a resumed WAITQ that must
not wait again for the already-retired division.

Reference behavior checked in PCSX2 v2.4.0:

- https://raw.githubusercontent.com/PCSX2/pcsx2/v2.4.0/pcsx2/VU1microInterp.cpp
  invokes pipeline flush when the E-bit delay completes.
- https://raw.githubusercontent.com/PCSX2/pcsx2/v2.4.0/pcsx2/VUops.cpp
  `_vuFlushAll` retires pending Q and advances to division completion.

## Validation

- Release tests: 2/2 passed.
- UBSan tests: 2/2 passed.
- Vita VPK build: passed (build validation, not a device execution test).
- Captured first VIF replay: unchanged, 998 pairs, 1163 modeled cycles,
  21 VF stall cycles, 144 Q stall cycles; PATH1 8 accepted / 1 rejected.
- Earliest rejection remains PC 00C0, kick 0120, bad tag address 0160,
  tag 00000000420F028B. Two triangles; no new visual claim.
- Local replay artifacts: `build-release/q-end-first-vif.txt` and `.ppm`.
  Firmware and captures remain uncommitted.

## Next work

Incremental XGKICK and GIF path arbitration remain the priority. Current kick
reads an entire packet immediately; the next tag is written after the kick.
Do not patch around this with a BIOS-specific delay or synthetic EOP. Establish
transfer timing independently and test producer/consumer ordering. Remaining
pipeline flush work includes VF/MAC and other unmodeled hardware pipelines.

Latest full BIOS visual evidence remains the earlier 300M-instruction run:
dark/gray rectangles, no recognizable intro. No new full BIOS run or physical
Vita performance test was performed in this checkpoint.

## Incremental XGKICK follow-up

Replaced issue-time whole-packet snapshots with pending, wrapped 16-byte reads
at two modeled VU cycles per quadword. The issue pair counts toward the first
transfer. Previously consumed bytes are retained; unread payload and subsequent
headers observe later RAM writes. Completed tags keep the existing GIF queue
interface. Host run-budget exhaustion preserves transfer state. A second kick
drains the previous transfer; E-bit completion drains the final transfer.
Additional drain cycles have their own `xgkick_stalls` diagnostic counter.

This is a bounded functional timing model, not cycle-accurate GIF arbitration:
no path priorities, backpressure, external EE-cycle scheduling or partial-tag
delivery to GS. The existing per-tag payload limit (0x3FF0 bytes) and 256-tag
chain limit remain deliberate safety bounds, not hardware restrictions.

Reference: PCSX2 v2.4.0 VUops.cpp `_vuXGKICKTransfer` and `_vuXGKICK` use
two-cycle quadword transfers and drain a prior kick before issuing another.
The implementation here is independently written around AstraRecomp's queues.

Tests cover delayed issue, payload modifications after header consumption,
16-KiB RAM wrap, immutable consumed headers, late chained headers, consecutive
kicks, an actual guest SQI producer, E-bit draining, cancellation on reset, oversized tags and bounded
unterminated chains. The DIRECT-order test now finishes its microprogram before
expecting queued PATH1 output; it no longer assumes issue-time snapshotting.

Isolated capture: `build-release/incremental-kick-first-vif.txt/.ppm`:
998 pairs, 1308 modeled cycles, 21 VF stalls, 144 Q stalls, 145 XGKICK drain
cycles. PATH1 remains 8/1 with the same first malformed tag at 0160 (origin
PC 00C0, kick 0120), detected at pair 55 instead of issue pair 52.
The later header write remains after rejection (pair 75, now cycle 82).
This does not establish that incremental transfer alone fixes the intro.

Final follow-up Release and UBSan tests both pass 2/2; Vita VPK builds.

Full replay completed at 300,000,000 instructions, reason=running (expected
exit 1 at the budget), EE PC 00272728 / opcode 2443000C. All 5,362 VIF
submissions accepted, zero rejected. GIF emits 34,810 triangles and zero lines;
VU1 still has 998 pairs and PATH1 8/1. Framebuffer hash remains
717B270A37FF96D5, with 10,240 nonzero RGB pixels out of 17,920.
Inspected `build-release/incremental-kick-300m.png`: unchanged dark/gray
rectangles, no recognizable intro. Local `.txt` and `.ppm` accompany it.
IOP reaches 37,500,000 cycles, PC 0000AE94 / opcode 08002BA5.

Next: investigate GIF arbitration and the first packet's missing terminator /
producer setup; preserve the evidence that the later header write still occurs
after the rejected read. Do not interpret later buffer reuse as proof that a
longer delay is correct. Audio remains paused and no physical Vita test was run.
