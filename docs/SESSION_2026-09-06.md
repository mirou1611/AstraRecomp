# Session checkpoint — September 6, 2026

## Corrected diagnosis and guest flat shading

Rechecked PRIM layout before changing shading: 0x5C selects triangle strip,
IIP=1 (interpolation), TME=1 (texturing), ABE=1 (blending), FGE=0 (no fog),
FST=0 (STQ coordinates), CTXT=0. The September 5 claim that this BIOS packet
requested flat shading was incorrect; the older notes now explicitly correct it.
The brightness gradient alone is not a rendering defect. Missing STQ texture
rendering and alpha blending are relevant gaps; fog is not enabled here.

Fixed the independent missing IIP=0 path: line and triangle raster submissions
use the final/kicking vertex's color for flat shading. Source vertices retained
for strips/fans and diagnostic records retain their original colors. IIP=1
continues through the existing interpolating backend. This patch does not claim
to fix the intro or change the previously captured PRIM=0x5C triangles.

Regression tests cover both IIP modes across line list, line strip, triangle
list, triangle strip and triangle fan, plus retained source colors after a flat
draw. Host suite passes 2/2. The existing captured-triangle coverage fixture
continues to pass (122 RGB pixels); no fresh full BIOS replay is claimed here.
Vita cross-build passes; VPK SHA-256:
`55144E5696EA168842DBB1C7C3C57F98A69949F4AA964611F9B337746D571043`.

Primary semantic references, inspected without copying implementation:
[GS register layout](https://github.com/PCSX2/pcsx2/blob/master/pcsx2/GS/GSRegs.h),
[primitive color setup](https://github.com/PCSX2/pcsx2/blob/master/pcsx2/GS/Renderers/SW/GSSetupPrimCodeGenerator.all.cpp).

## Resume priorities

Continue from the raw-coordinate capture documented in
[the previous session](SESSION_2026-09-05.md). Two guest triangles arrive with
identical vertices; three narrow triangles account for the observed RGB output.
Trace VU construction and ADC decisions rather than inferring correctness from
pixel counts. Investigate the rejected XGKICK tag at VU data offset 0x160.
Account for STQ texture sampling and blending when evaluating the captured image.
Audio remains DMA/memory groundwork without synthesis or Vita speaker output.
