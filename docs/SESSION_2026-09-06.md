# Session checkpoint — September 6, 2026

## Corrected diagnosis and guest flat shading

Rechecked PRIM layout before changing shading: 0x5C selects triangle strip,
IIP=1 (interpolation), TME=1 (texturing), ABE=1 (blending), FGE=0 (no fog),
FST=0 (STQ coordinates), CTXT=0. The September 5 claim that this BIOS packet
requested flat shading was incorrect; the older notes now explicitly correct it.
The brightness gradient alone is not a rendering defect. Missing STQ texture
rendering and alpha blending are relevant gaps; fog is not enabled here.

Fixed the independent missing IIP=0 path: line and triangle raster submissions
use the final/kicking vertex's color for flat shading. Source vertices retained
for strips/fans and diagnostic records retain their original colors. IIP=1
continues through the existing interpolating backend. This patch does not claim
to fix the intro or change the previously captured PRIM=0x5C triangles.

Regression tests cover both IIP modes across line list, line strip, triangle
list, triangle strip and triangle fan, plus retained source colors after a flat
draw. Host suite passes 2/2. The existing captured-triangle coverage fixture
continues to pass (122 RGB pixels); no fresh full BIOS replay is claimed here.
Vita cross-build passes; VPK SHA-256:
`55144E5696EA168842DBB1C7C3C57F98A69949F4AA964611F9B337746D571043`.

Primary semantic references, inspected without copying implementation:
[GS register layout](https://github.com/PCSX2/pcsx2/blob/master/pcsx2/GS/GSRegs.h),
[primitive color setup](https://github.com/PCSX2/pcsx2/blob/master/pcsx2/GS/Renderers/SW/GSSetupPrimCodeGenerator.all.cpp).

## Resume priorities

Continue from the raw-coordinate capture documented in
[the previous session](SESSION_2026-09-05.md). Two guest triangles arrive with
identical vertices; three narrow triangles account for the observed RGB output.
Trace VU construction and ADC decisions rather than inferring correctness from
pixel counts. Investigate the rejected XGKICK tag at VU data offset 0x160.
Account for STQ texture sampling and blending when evaluating the captured image.
Audio remains DMA/memory groundwork without synthesis or Vita speaker output.

## XGKICK origin capture and complete primitive scissor coverage

Oversized-tag diagnostics now preserve the originating VU PC, kick start,
zero-based tag index, preceding tag and a 128-byte snapshot from the kick origin.
Tests distinguish a later-chain rejection from the initial tag, verify a
nonzero originating PC, preserve bytes across subsequent writes, and clear
metadata on reset. This capture is bounded and does not enlarge accepted packets.

Replay `build-release/xgkick-snapshot-2488m.txt` (without census collection)
retains the 248.8M-step hash `3F7CE0BA02868615` and 122 RGB pixels. The rejected
transfer starts at VU PC 0x00C0, kick data offset 0x0120. At that address the tag
is `1000000000000002`, descriptor E: two PACKED A+D entries, no EOP. Both entries
at 0x130/0x140 are zero. The next tag at 0x150 is zero (NLOOP=0, EOP=0).
Parsing then reaches `00000000420F028B` at 0x160 as tag index 2 and exceeds the
snapshot limit. The following words include plausible floating-point transform
data. The earlier speculation that these bytes are instructions is not established.
The initial VU program loads VF31 from qword 0x15 and VF1..4 from qwords 0x16..19;
the absent terminator is therefore adjacent to the initial parameter vectors.
Next trace the VIF upload/CPU construction of qwords 0x12..0x15. Do not treat
zero NLOOP as EOP without hardware-semantic evidence or increase the packet cap.

Independently fixed SCISSOR coverage: points, lines and triangles now apply the
selected guest scissor, not just sprites. Rejection happens before depth/color
writes. Tests cover each primitive, context switching and inverted guest bounds
that would otherwise collapse under quarter-scale division. The existing coarse
coordinate scaling is retained; this is not subpixel-accurate rasterization.
The captured BIOS triangles are all inside their scissor bounds. The full replay
above preceded this independent scissor change; its focused tests pass.

Host suite passes 2/2 and Vita cross-build passes. VPK SHA-256:
`A040556C145E26A87A9E3FA680BE0D5BC197E14111E2E6133032A9C398FAE1F4`.

## End-of-day handoff — user requested pause

Work paused on September 6 at the user's request; resume later, not in the
background. Latest implementation checkpoint: `ee3f6df` on
`research/spu2-dma-checkpoint`, pushed to origin. Earlier checkpoints today:
`29f852f` (flat shading and corrected flag diagnosis), followed by `ee3f6df`
(XGKICK origin/snapshot diagnostics and primitive scissor coverage).

Start the next session by inspecting git status and this handoff. Preserve the
user's untracked `ASTRARECOMP_UNCONVENTIONAL_OPTIMIZATION_RESEARCH_PLAN.md` and
`Coworker1337.txt`; neither belongs in an incidental commit. BIOS and diagnostic
captures remain local. The public main README was updated separately; do not
merge the entire research branch into main as part of resuming.

Immediate task: trace VIF UNPACK inputs and CPU-side construction of VU data
qwords 0x12 through 0x15 before the first MSCAL/XGKICK at micro-PC 0x00C0.
Determine why the packet at byte offset 0x120 has no terminating EOP before
parsing reaches transform-like data at 0x160. Keep the packet-size guard intact.
Use `build-release/xgkick-snapshot-2488m.txt` for the exact captured bytes.

Verified state: host tests 2/2 passing, Vita cross-build passing, 998 VU pairs,
five emitted triangles and 122 nonzero RGB pixels in the last full replay.
Two triangles are already degenerate in guest coordinates; the remaining three
produce narrow fragments. Full replay predates the separately tested scissor
fix. No recognizable PS2 intro, playable retail games, generated audio or measured
physical-Vita performance is established. Do not assign a completion percentage
from instruction, primitive or pixel counts; prior precise estimates are not
reliable. Continue with small, tested fixes and push documented checkpoints.
