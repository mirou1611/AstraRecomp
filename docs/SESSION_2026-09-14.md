# Session checkpoint — 2026-09-14

## Requested 800M instruction run completed

The run started on September 13 and its completed results were inspected on
September 14. No emulator behavior was changed for this experiment. Local files:
`build-release/extended-800m.txt`, `.ppm`, and inspected `.png`.

- EE: 800,000,000 instructions, reason=running (requested ceiling, not a crash).
  PC 00216E40, opcode 27BD0010. No unsupported EE instruction stopped the run.
- IOP: 100,000,000 cycles, reason=running, PC 0000AE98 / opcode 00000000.
- VIF1: 59,706 submissions, zero rejected.
- GIF: 59,964 packets, zero rejected, zero pending bytes; 384,780 triangles,
  2,808 lines, 2,157 sprites. Image/local bytes: 368,640.
- VU1: still 998 pairs, stopped at PC 0308 with no unsupported instruction.
  PATH1 remains 8 accepted / 1 rejected; the known initial tag issue persists.
- Framebuffer hash: 1787AA7598BE9291. Nonzero RGB pixels: 10,240 / 17,920.

Visual inspection shows gray rectangular regions, a small dark polygon, and a
black lower area. It is **not a recognizable PS2 intro**. More executed
instructions and more triangles establish longer execution coverage, not correct
rendering, game compatibility or viable Vita performance. This was a host replay,
not execution on physical Vita hardware. No new audio work was performed.

## Interpretation and next work

The project can reach the requested 800M instruction ceiling with its current
functional implementation. The display changes after the 400M checkpoint, but
the major visible milestone is still unmet. Later graphics traffic grows while
VU1 stays at 998 pairs. Do not assume that the first rejected VU packet explains
all missing visuals, or keep extending the instruction ceiling as a substitute
for checking the rendering pipeline.

Next inspect the later GIF/GS draw data and renderer coverage, particularly the
known missing ST/Q handling and textured-triangle rendering. Use independent
fixtures and the existing causal/differential approach before claiming a fix.
Event-based arbitration remains an open correctness task; no arbitrary delay or
synthetic EOP is authorized as a shortcut.

Main goal: recognizable PS2 intro on Vita. Audio remains paused. Research branch:
`research/spu2-dma-checkpoint`. Existing development checkpoint is in
SESSION_2026-09-13.md. This checkpoint changes documentation only; no new build
or unit-test claim is needed. Firmware and captured images/logs stay local and
uncommitted. User coworker files remain untouched.

## Follow-up: GIF texture attribute latches

Implemented the missing PACKED STQ handler. ST is written immediately; Q goes
to a separate temporary latch and is copied into RGBAQ only by PACKED RGBA.
Each nonempty processed GIFtag resets temporary Q to the raw float-one bits.
A+D and REGLIST ST leave RGBAQ.Q alone. XYZ now snapshots raw ST, UV and Q in
GsVertex, preserving attributes through existing list/strip/fan assembly.

Reference checked: PCSX2 v2.4.0 GSState.cpp, GIFPackedRegHandlerSTQ,
GIFPackedRegHandlerRGBA, GIFRegHandlerST and Transfer tag initialization:
https://raw.githubusercontent.com/PCSX2/pcsx2/v2.4.0/pcsx2/GS/GSState.cpp
This implementation retains raw bits; it deliberately does not copy reference
renderer zero/NaN substitutions as if they were verified hardware behavior.

Fixtures cover split input, STQ without RGBA retaining the earlier Q, explicit
A+D RGBAQ, per-vertex latching, new-tag temporary-Q reset, REGLIST ST/UV and
reset. Release CTest 2/2 and UBSan CTest 2/2 pass; Vita VPK builds. The first-VIF
replay still exits at the known rejected PATH1 tag (local stq-latch-first.txt).
No new 800M replay was performed for this state-only change.

This does NOT implement texture sampling for triangles or establish a visible
intro improvement. Next: use these latched attributes in a tested textured
triangle raster path, checking texture format/addressing coverage before a long
BIOS visual comparison. Audio remains paused.

## Follow-up: fixed-UV textured triangles

GIF triangles with TME=1 and FST=1 now call the existing logical-local-memory
texture sampler for covered fragments. The GS rasterizer interpolates raw 10.4
UV before truncating to texel coordinates, keeps attributes attached when winding
is reversed, and sends sampled color through existing alpha/depth/blend output.
Untextured triangles retain their old path. This is an affine, nearest-sample
implementation, not the perspective ST/Q path.

The rendering-stage validation approach was guided by the 3d-games skill; the
PS2 reference inspected was PCSX2 v2.4.0 GSDrawScanline.cpp CSetupPrim, which
distinguishes fixed UV from interpolated STQ:
https://raw.githubusercontent.com/PCSX2/pcsx2/v2.4.0/pcsx2/GS/Renderers/SW/GSDrawScanline.cpp
Independent fixtures upload a four-color 2x2 texture and draw two triangles in
both windings and both GS contexts. Tests check shared-edge coverage, fractional
UV interpolation and alpha-test rejection after sampling.
Validation: Release CTest 2/2, UBSan CTest 2/2, and Vita VPK build pass.

Limitations inherited from the sampler remain: logical linear PSMCT32/16 memory,
no GS swizzle/CLUT, no CLAMP modes or filtering, incomplete TFX/TCC/TEXA semantics.
ST/Q triangles still use the untextured path. No BIOS visual improvement is
claimed and no new long BIOS replay was run. Next implement and test perspective
ST/Q plus safe nonfinite handling, then inspect actual BIOS texture state and
compare captured output. Do not mistake passing synthetic fixtures for an intro.

## Coworker priority check: VU1 termination settled locally

Replayed first-vif.bin with an unconditional exit summary and a five-pair static
micro-memory window in ps2vif_replay (local vu-exit-first.txt/.ppm). Result:
running=0, PC=0308, unsupported upper/lower both zero, 998 issued pairs.

- 02F8: lower 8000033C (NOP), upper 400002FF (NOP with E bit).
- 0300: lower 8000033C / upper 000002FF (delay-pair NOPs).
- 0308: lower 400007BF (B to 0108), upper NOP.
- 0310: NOP pair, the resume branch delay pair.

In the current interpreter, budget exhaustion leaves running=true. Unsupported
instructions stop with diagnostic words; the successful stopping path is E-bit
retirement. Thus this captured endpoint is normal termination, not a flag loop
or the two-million-pair ceiling. The static window alone is not an execution
trace; the conclusion also uses running/unsupported state and the stop logic.
An independent minimal regression checks E+delay stops at 0308 and resume takes
the branch to 0108 after its delay pair.

Existing invocation records show MSCAL followed by SIX MSCNTs, ending at pair
55, 215, 375, 532, 686, 846 and 998 respectively. The 800M log likewise reports
running=0, PC=0308 and no unsupported words. This rules out "MSCNT never fires"
within the first packet, not missing later launches or other VU correctness
bugs. No CPU/VU timing behavior was changed in this checkpoint.
Validation: Release CTest 2/2 and UBSan CTest 2/2 pass. Replay returns the known
PATH1-rejection exit code 1; no new rejection was introduced. Only host-tool,
test and documentation files changed, so no new Vita build was needed.

Next reference prerequisite: no GS dump was found in the workspace search.
PCSX2 v2.4.0 GSDump.h describes a saved-state header, privileged registers and
transfer/vsync/readback/register events, not a naked GIF stream:
https://raw.githubusercontent.com/PCSX2/pcsx2/v2.4.0/pcsx2/GS/GSDump.h
A faithful importer must restore compatible initial state and texture memory;
otherwise use a self-contained homebrew fixture with explicitly matched state.
The current 160x112 logical GS backend also needs matched resolution/coverage
before a pixel difference can be classified as an arithmetic error. No golden
comparison has been performed yet. Physical Vita measurements remain deferred.

## Isolated PATH3 integration gate

Added tests/path3_graphics_tests.cpp as its own CTest target. A seven-instruction
EE program writes D2_MADR/QWC/CHCR; a bounded device-clock advance completes DMA,
then Emulator::service_graphics delivers the packet to GIF/GS. The test uploads
a 2x2 color texture and draws one fixed-UV triangle covering a 32x32 half-square.
It compares all 17,920 framebuffer pixels against an independently specified
coverage/color formula (including background), checks DMA completion and verifies
zero VIF submissions and zero VU pairs. It does not call Gif::submit directly.

Optional image output:
`./build-release/path3_graphics_tests build-release/path3-triangle.ppm`
Local PPM converted to PNG and visually inspected: red/green/blue textured
triangle at upper left, black background; zero pixel mismatches. This is a
logical-renderer regression oracle, NOT a PCSX2 or hardware golden result, and
is not yet a standalone homebrew ELF. It establishes that this narrow
EE-store -> PATH3 DMA -> GIF -> texture/raster path functions independently of
VIF/VU; it does not prove all GS operations or the BIOS intro correct.

No emulation implementation changed. Initial fixture mistakes (signed MMIO
store offset and missing DMA completion wait) were corrected before acceptance.
Next: package a reference-compatible self-contained ELF with full GS display
state, or obtain a dump with a supported initial-state restoration path. Keep
the distinction between synthetic expected pixels and hardware truth explicit.
Validation: Release CTest 3/3 and UBSan CTest 3/3 pass. The new executable is
host-only; no new physical Vita result or Vita build is claimed.

## PATH3 fixture now executes as ELF

The integration gate now constructs an explicit little-endian ELF32 MIPS image
with entry 00100000 and aligned packet data at 00101000, loads it through
Emulator::load_elf, and executes via run_slice. Guest LW/ANDI/BNE polling waits
for D2_CHCR.STR to clear, then BREAK signals test completion. A 10,000-instruction
host ceiling detects a stuck poll. Removed direct host RAM seeding and the
host-side memory.advance completion loop from this gate.

Export the tested artifact and image together:
`./build-release/path3_graphics_tests build-release/path3-elf.ppm build-release/path3-triangle.elf`

readelf confirms ELF32 little-endian MIPS EXEC, entry 00100000, one RX LOAD
segment of 0x11f0 bytes with 16-byte alignment. The exported PPM is byte-identical
to the previous passing PATH3 image and all framebuffer pixels match.

Important boundary: this is a standalone HOST-TEST ELF, not yet a portable PS2
display demo. It intentionally uses BREAK rather than a persistent display
loop and does not initialize privileged display registers, FRAME/ZBUF/PRMODECONT,
global DMAC enable or cache synchronization for an external runtime. Do not
present it as a working PCSX2/hardware golden fixture. Reference-ready display
setup remains next; no GS dump comparison or new intro result is claimed.
Validation: Release CTest 3/3 and UBSan CTest 3/3 pass; only host test and
documentation changed. Firmware and generated ELF/image artifacts stay local.
