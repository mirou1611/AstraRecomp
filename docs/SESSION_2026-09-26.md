# Session 2026-09-26: draw-level BIOS feedback checkpoint

Main goal remains a recognizable PS2 intro. Audio stays deferred. This is a
graphics diagnostic checkpoint, not an intro or physical-Vita result.

The corrected 300M-step display probe reached `PC=00272728`, with GIF packet
count 5418, VU1 pair count 998, draw-preview hash `565638DCA404F0B3`, and
linear display-probe hash `377325F64EDED69D`. The explicit DISPFB linear
address helper preserves those hashes. Its compile-time regression test pins
FBP `0x50` to byte address `0xA0000`, exactly 64 quarter-grid rows after the
zero-base draw preview. Release CTest remains 14/14.

Bounded GIF sprite tracing now records raw XYZ/UV pairs and selected GS state,
including TEX1. In the 300M replay, the first 191 emitted sprites all fit
within the 256-record trace bound. Sprite sequence 11 has the same full-screen
vertex/UV values and key GS state as the PCSX2 software-renderer draw 99
reference: `PRIM=0x116`, `TEX0=0x628128000`, `TEX1=0x60`,
`FRAME=0xA00F0`, and `TEXA=0x807F`. The signature repeats on later Astra
sprites, so sequence 11 is **not** claimed to be the same frame or unique draw.
The independently captured PCSX2 draw context and image remain under the
ignored `.tools/pcsx2-reference-data/bios-first-draws` directory.

Astra's target immediately after sprite 11 has zero nonblack RGB pixels. A
second capture proved its raw linear source-texture grid was also all black
(0/10,240 nonblack samples), so bilinear filtering cannot explain this
particular black pass. PCSX2 draw 99's input texture has 4,031 nonblack RGB
samples on the equivalent first-640-pixel quarter grid. These observations
are **not yet lockstep** because the signature recurs across frames.

The tracer now has a bounded per-sprite source/target hash and nonblack census.
The 300M-step replay reached the same endpoint, packet counts, and final
framebuffer/display hashes. Repeated feedback sprites 11/13/15/17/19 have
0/10,240 nonblack source samples and black targets. The same state and
geometry at sprites 48/50/52/54/56 have 10,240/10,240 nonblack source samples
and 10,017/17,920 nonblack target samples. The same source count persists in
later repeats. A targeted 275M-step replay captured sprite 48's source and
post-draw target. The source is blue/noisy across the entire sampled 160x64
grid; the target copies that full-width texture into its upper region. Local
PPMs `build-release/sprite-48-source.ppm` and
`build-release/sprite-48-target.ppm` are ignored and not committed. Their
hashes `DB0A9D10132D24CE` and `108C25DFDA9A0040` match the independent
300M sprite census. RGB comparison against the PCSX2 draw-99 input texture's
first 640x256 region, sampled every fourth pixel, gave 0/10,240 exact
matches, 10,240 versus 4,031 nonblack pixels, and mean RGB `(23,37,100)`
versus `(1,3,9)`. This is a real surface-content discrepancy between
captured passes with the same GS sprite signature; exact frame lockstep is
still unproven.

Do not fix sprite 48 by changing TEX1's ignored linear-filter setting:
filtering cannot manufacture the missing black/cloud mask in its already
incorrect input surface. The next reference-backed step is to align the
source-producing draws before this sprite with PCSX2's draw-98 context and
image, then check FRAME, alpha/blend, coverage and texture inputs. Neither
VU clipping nor GIF scheduling is singled out by this evidence. The
diagnostic does not alter guest arithmetic or GS writes; target probes use
`Gs::pixel` rather than refreshing the renderer's preview cache.

The CLI now consistently interprets `-` as skipping any optional output path.
An accidentally generated file literally named `-` was moved into the ignored
`build-release/diagnostic-dash.ppm` path, not deleted. The user-owned research
plan and coworker files remain untouched and untracked. Release and UBSan
CTest both pass 14/14, and the Vita VPK cross-build passes; no hardware
runtime result is claimed.
