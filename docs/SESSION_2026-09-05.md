# Session 2026-09-05: GS local-memory and texture oracle

## Outcome

The GIF frontend now preserves split IMAGE transfers across DMA submissions,
honors GIFtag PRE/PRIM state, and assembles the common GS primitive topologies.
The corrected 248.8-million-step BIOS replay parses all 96 GIF submissions with
zero rejection and zero bytes left pending: 83 packed tags, 13 IMAGE tags, and
368,640 raw image bytes.

Transfer-state capture proves that the IMAGE workload is coherent rather than
corrupt VU output. It consists of PSMCT32 and PSMCT16 host-to-local surfaces at
GS bases `0x3200` through `0x3760`, with dimensions from 64x64 to 256x256. The
declared formats and rectangles account for every payload byte exactly.

A heap-backed 4 MiB logical GS local-memory oracle now applies DBP, DBW,
TRXPOS, TRXREG, and TRXDIR for the observed PSMCT32/PSMCT16 transfers. The real
replay stores all 368,640 bytes. TEX0 context state, UV coordinates, nearest
PSMCT32/PSMCT16 sampling, and basic MODULATE/DECAL handling are connected to
textured sprites. Focused tests validate split DMA traversal, 2D local-memory
row stride, and an end-to-end four-color textured sprite.

The replay remains at framebuffer hash `AE6BFFA9F3064B83` with `0/17920`
nonzero pixels. This boundary contains two genuine untextured sprites and no
point, line, or triangle draws, so the remaining black frame is not evidence of
a failed texture sample. The next frontier is privileged GS display/framebuffer
selection or advancing to the later draw command that consumes these surfaces.

## Validation

- Host build: passes.
- Host tests: 2/2 pass.
- BIOS replay: VIF rejection 0, GIF rejection 0, VU1 stops cleanly at TPC
  `0x308`, all 368,640 IMAGE bytes stored.
- VitaSDK cross-build: passes. `build-vita/ps2vita.vpk` SHA-256 is
  `EB139C063EE0792CE4DB86173D57DE07FEEF51920EB9182C422B4C8C495C4654`.
