# AstraRecomp session handoff — 2026-08-29

## Milestone reached

The Phase-0 pipeline now uses genuine PS2Recomp analyzer/emitter output rather
than a hand-lowered placeholder.

- Built `ps2_analyzer` and `ps2_recomp` from the isolated WSL checkout at commit
  `14b1e5cb39b4af7e6fc12f9a29fdc751efde49d7` using locally staged dependencies.
- Analyzer found one function at `0x1000` in the generated smoke ELF.
- Recompiler processed and emitted that function with zero decode failures,
  unhandled instructions, guest fallbacks, warnings, or errors.
- Adapted the emitted instruction semantics to the small AstraRT ABI in
  `src/core/aot.cpp`, preserving PC checkpoints and treating BREAK as a terminal
  AstraRT boundary.
- Host differential tests pass.
- VitaSDK VPK build passes.
- Vita3K visibly reports `PS2R AOT T0: PASS INTERP:42 NATIVE:42` while the BIOS
  bootstrap path continues running.
- Added the AstraRT exact-entry function table and explicit native `Direct`,
  `Indirect`, `Interpreter`, and `Stop` exits.
- Added a two-function corpus with JAL, a delay slot, resumable caller entry, and
  `jr $ra`. PS2Recomp found two functions plus one resumable entry with zero
  errors or fallbacks. Host differential testing and Vita3K both pass T1.

Evidence screenshot:
`C:\Users\amir\AppData\Local\Temp\astrarecomp-ps2r-pass.png`.

Latest CALL/RET evidence screenshot:
`C:\Users\amir\AppData\Local\Temp\astrarecomp-t1-call-ret-vita3k.png`.

Latest package-dispatch evidence screenshot:
`C:\Users\amir\AppData\Local\Temp\astrarecomp-package-check.png`.

VPK:
`C:\Users\amir\Desktop\PS2 vita\build-vita\ps2vita.vpk`.

Reproducibility hashes:

```text
FD11C250347DA780E0D1FE758884428D42EC505D9A347A694B55FA9818E02341  astrarecomp-smoke.elf
E52A26F79F51FEFCBF0816610C54A9406D900BDF58A4EE73CC0F0ABB57D43AFD  astrarecomp-smoke.toml
95421D7FAB14DB033DA25504773A679CAD5292040397F88E2287FBC12E23B9AA  sub_00001000_0x1000.cpp
```

An earlier build inlined T0 into its probe wrapper. The function-pointer package
boundary now keeps translated bodies independently measurable. The Vita ARM
object reports:

```text
T0 translated body       136 bytes
T1 caller/resume body    292 bytes
T1 leaf body             112 bytes
binary entry lookup       76 bytes
single-function execute   98 bytes
bounded dispatcher       156 bytes
entry table               48 bytes (4 exact entries)
package descriptor        12 bytes
```

Latest VPK is 132,064 bytes with SHA-256:

```text
A719E6C46DBD69EB7BF2C4F2F9994CFB8D52F63E29E5AAA770626C2E9F803BD3
```

## Upstream test caveat

The two required host tools build successfully. The aggregate upstream test
target does not build when `PS2X_BUILD_RUNTIME=OFF`, because runtime tests are
still wired in and include runtime-only headers (`Stubs/Unimplemented.h` and
`ps2x/iop/iop_types.h`). This is an upstream CMake test-configuration issue, not
an analyzer/recompiler failure. AstraRecomp's own differential suite is green.

## Reproduce

```bash
./scripts/run-ps2recomp-smoke.sh
./scripts/run-ps2recomp-chain.sh
cmake --build build-host -j4
ctest --test-dir build-host --output-on-failure
./scripts/build-vita.sh
```

## Next engineering target

Replace the single hard-coded probe adaptation with a generated AstraRT backend
for a small supported instruction subset. The first useful slice is arithmetic,
loads/stores, direct branches/calls, and explicit exits. Generate a function table
and compile it into the Vita build, then differentially fuzz short basic blocks
against the interpreter before attempting a BIOS module or game overlay.

## End-of-day checkpoint

Session paused deliberately on 2026-08-29 after the package-dispatch milestone.
No build or test is currently known to be failing.

Final verified state:

- host build completed successfully;
- `ps2core_tests`: 100% passed, 0 failed;
- VitaSDK cross-build completed successfully;
- latest VPK installed and launched in Vita3K;
- Vita monitor visibly showed both T0 and T1 as PASS while the BIOS path ran;
- `AotPackage` uses allocation-free binary lookup over exact callable/resumable
  guest entries;
- bounded native dispatch safely yields to the interpreter;
- T1 matches the interpreter across JAL, delay slot, translated leaf execution,
  `jr $ra`, resumable caller execution, RAM output, cycles, PC, and BREAK;
- reproducible PS2Recomp scripts exist for both the single-function and call-chain
  corpora;
- coworker review handoff is saved as
  `docs/COWORKER_REVIEW_2026-08-29.md`.

Final visible evidence:

```text
C:\Users\amir\AppData\Local\Temp\astrarecomp-package-check.png
```

Final VPK:

```text
C:\Users\amir\Desktop\PS2 vita\build-vita\ps2vita.vpk
SHA-256 A719E6C46DBD69EB7BF2C4F2F9994CFB8D52F63E29E5AAA770626C2E9F803BD3
```

### First action next session

Implement package validation and the first automatic AstraRT code-generation
slice. Start with `ADDIU`/`ADDU`, 32-bit loads/stores, JAL/JR with delay slots,
BREAK, and explicit interpreter exits. Generate a package table instead of adding
another manually adapted function. Differentially compare every generated corpus
against the interpreter before applying the backend to BIOS code.

After that backend is reliable, return to the PS2-intro path: capture the current
BIOS MMIO wait loop, identify the missing IOP/SIF/interrupt event, implement it,
and iterate until the first GIF traffic is observed.
