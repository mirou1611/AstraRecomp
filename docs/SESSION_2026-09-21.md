# Session 2026-09-21: reference-verified framebuffer feedback reproducer

Added `path3_graphics_tests --feedback [PPM] [ELF] [LOOP_ELF]` as a diagnostic
variant. It currently deliberately returns failure on Astra; it is not registered
as a passing CTest or hidden behind WILL_FAIL. No core behavior changed.

The guest executes PATH3 DMA through EE stores, with no VIF/VU involvement:

1. Clear target B (base0x40000, width640, PSMCT32).
2. Draw an8x8 red sprite into A (base0x80000, width64, PSMCT32).
3. Sample A as PSMCT24 into B's left8x8 region.
4. Overwrite A with blue and sample it into B's adjacent right8x8 region.

The old left region must remain red. This checks target isolation and visibility
of subsequent writes, not just copying an uploaded texture. Both copies use
nearest filtering, DECAL/RGBA and TEXA.TA0=64,AEM=0; guest EQUAL64 alpha test
makes the generated texture alpha observable in RGB output. Source draw alpha
is128, so simply retaining source alpha is insufficient. The original2x2 upload
in the shared fixture remains at0x100, unrelated to source A.

Host expectation: eight quarter-grid pixels (red2x2 then blue2x2), alpha64,
with black elsewhere. Current Astra: FAIL, pixel_mismatches=8. Existing13 CTest
tests pass in Release and UBSan. Persistent-loop export and missing-clear negative control
remain exercised by this diagnostic variant, but the whole variant stays failed
until the independent pixel expectation is satisfied.

## Executable reference

PCSX2v2.8.2 software renderer, isolated profile, same exported LOOP_ELF:
`.tools/pcsx2-reference-data/feedback-draws/00005_f00010_rt1_00400_C_32.png`.
`check_path3_reference.ps1 -Feedback -ImagePath <file>` reports0/128 nativeRGB
mismatches. Image inspected: red left, blue right. The check covers the16x8
draw region, not the entire framebuffer. Reference process2272 stopped after
capture with executable path checked. No firmware or captures committed.

LOOP_ELF SHA256:
5ADD12C2F894339D70A317648C7DB91035699DEE9E987288460A22181292179B

Local exports: build-release/feedback.ppm, feedback.elf, feedback-loop.elf.
Reference profile now writes to feedback-draws. The 3d-games skill informed
isolating sampling and output routing in this two-target test.

## Next implementation boundary

Implement shared framebuffer/texture color storage and explicit target routing,
then PSMCT24/TEXA sampling; rerun this fixture until it genuinely passes. Preserve
standalone Gs tests and the quarter-resolution contract. Do not patch by copying
the BIOS screenshot or treating a host image as native VRAM. Shared addressing,
frame masks, target aliases and display selection need explicit scope; this
fixture alone will not prove their general correctness. No new BIOS replay or
intro claim this session. Vita runtime sources are unchanged.
